import React from 'react';

const ProductDetail = ({id, name}) => {
    return ( 
        <div>
            <div>
                <span>{name}</span> 
            </div>
        </div>
     );
}
 
export default ProductDetail;